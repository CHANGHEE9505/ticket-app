# k8s/backend.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: clouddxuser/ticket-backend:latest
        imagePullPolicy: Always  # 👈 중요! 무조건 최신 이미지를 새로 받아오게 하는 설정
        ports:
        - containerPort: 3000    # 👈 중요! 백엔드 코드와 똑같이 3000번으로 통일
---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 80          # 외부(로드밸런서)는 80번
      targetPort: 3000  # 내부(백엔드)는 3000번으로 연결
  type: LoadBalancer